params {
	output_dir = "results"
	rawfile_dir = "raw_files"
	meta_file = "sample.csv"
	reference_genome = "rabies_virus.fna"
	rawfile_type = "fastq"
}

conda.enabled = true

executor {
	name = 'local'
	queueSize = 5 // Limits the executor to run one process at a time
}

